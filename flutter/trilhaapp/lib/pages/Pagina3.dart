import 'package:flutter/material.dart';

class Pagina3MyWidget extends StatelessWidget {
  const Pagina3MyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurple,
    );
  }
}
