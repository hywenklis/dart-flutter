import 'package:flutter/material.dart';

class Pagina2MyWidget extends StatelessWidget {
  const Pagina2MyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.amber);
  }
}
