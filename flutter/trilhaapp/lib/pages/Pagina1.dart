import 'package:flutter/material.dart';

class Pagina1MyWidget extends StatelessWidget {
  const Pagina1MyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
          color: Colors.grey,
        );
  }
}